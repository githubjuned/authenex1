runtime: nodejs18
env: standard
instance_class: F1
automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 0
  max_instances: 1

env_variables:
  NODE_ENV: 'production'
  # Add your API keys here or use Secret Manager
  # GEMINI_API_KEY: "your-key"
  # FIREBASE_PROJECT_ID: "your-id"
